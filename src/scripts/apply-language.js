// Inlined into <head> via is:inline BEFORE any stylesheet. Reads
// localStorage.lang and sets document.documentElement.lang before first
// paint, preventing a flash of English for returning Serbian visitors.
// Built in Phase 2.4.
