# Testimonials — intentionally empty at launch

Per App Flow 4.4 and PRD FR-4: fewer than two strong named quotes is worse
than none, so this collection ships empty and the section does not render.

When two or more quotes with written permission exist, add them here with
`published: true`. The section then appears between skills and contact with
no code change.
