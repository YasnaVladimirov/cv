---
// S2 — case study page. Prerenders one route per published case study.
// Built in Phase 4.5. Returning [] here means no routes exist yet, so any
// /work/* request falls through to the 404 page.
export async function getStaticPaths() {
  return [];
}
---
