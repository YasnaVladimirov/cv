// Translation helpers and the client-side language store.
// Built in Phase 2.4: getDictionary(lang), t(key, lang), a React context +
// useI18n() hook, and a module-scope store that persists to localStorage,
// updates document.documentElement.lang, and fires the lang_switch event.
export {};
