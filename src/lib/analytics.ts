// Single trackEvent(name, props?) entry point mapping to the analytics
// provider chosen in Phase 6.3. No-ops silently when unconfigured — an
// analytics failure must never affect page function (PRD FR-7).
export {};
